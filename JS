<!DOCTYPE>
<html>
<body>

<script>
	  var firstFunck = function( x, y, z ){
	          if (x + y > z && y + z > x && z + x > y ){
	          var sum = x + y + z;
	             alert(sum)
	             }
	             else{
	             console.log( 'error')
	             }
	             }
	             firstFunck(3, 4, 2)
	
  </script>

</body>
</html>
